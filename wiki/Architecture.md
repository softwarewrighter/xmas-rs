# Architecture

This page provides a comprehensive overview of the xmas-rs architecture, including system design, component relationships, and data flow.

## System Architecture Overview

The xmas-rs application follows a clean, layered architecture built on the Yew framework's component-based model.

### High-Level Architecture

```mermaid
graph TB
    subgraph Browser
        UI[User Interface - HTML/CSS]
        WASM[WebAssembly Runtime]
    end

    subgraph YewFramework[Yew Framework]
        APP[App Component]
        STATE[State Management]
        RENDER[Virtual DOM Renderer]
    end

    subgraph RustCode[Rust Code]
        MAIN[main.rs]
        LIGHTCOLOR[LightColor Enum]
        CALLBACKS[Event Callbacks]
    end

    subgraph WebAPIs[Web APIs]
        DOM[DOM Manipulation]
        INPUT[HTML Input Element]
    end

    UI --> WASM
    WASM --> APP
    APP --> STATE
    APP --> RENDER
    RENDER --> DOM
    STATE --> RENDER
    CALLBACKS --> STATE
    MAIN --> APP
    LIGHTCOLOR --> APP
    INPUT --> CALLBACKS
    DOM --> UI
```

### Component Architecture

```mermaid
graph LR
    subgraph AppComponent[App Component]
        STATE_MGMT[State Management]
        EVENT_HANDLERS[Event Handlers]
        RENDER_LOGIC[Render Logic]
    end

    subgraph State[Application State]
        IS_PLAYING[is_playing: bool]
        SPEED[speed: i32]
        INPUT_REF[speed_input_ref]
    end

    subgraph Events[Event Callbacks]
        ON_PLAY[on_play]
        ON_STOP[on_stop]
        ON_SPEED[on_speed_change]
    end

    subgraph Rendering[Render Output]
        LIGHTS[8 Light Elements]
        CONTROLS[Control Buttons]
        TITLE[Title Element]
    end

    STATE_MGMT --> State
    EVENT_HANDLERS --> Events
    RENDER_LOGIC --> Rendering
    Events --> State
    State --> Rendering
```

## Data Flow Architecture

The application follows a unidirectional data flow pattern:

```mermaid
flowchart TD
    USER[User Interaction] --> EVENT[Event Triggered]
    EVENT --> CALLBACK[Callback Handler]
    CALLBACK --> STATE[Update State]
    STATE --> RERENDER[Trigger Re-render]
    RERENDER --> VDOM[Update Virtual DOM]
    VDOM --> DOM[Update Real DOM]
    DOM --> CSS[Apply CSS Animations]
    CSS --> DISPLAY[Display Updated UI]
    DISPLAY --> USER
```

### State Management Flow

```mermaid
stateDiagram-v2
    [*] --> Initialized
    Initialized --> Playing: Auto-start
    Playing --> Paused: on_stop clicked
    Paused --> Playing: on_play clicked
    Playing --> Playing: Speed changed
    Paused --> Paused: Speed changed

    state Playing {
        [*] --> Speed1
        Speed1 --> Speed2: Change speed
        Speed2 --> Speed3
        Speed3 --> Speed4
        Speed4 --> Speed5
        Speed5 --> Speed1
    }
```

## Layer Architecture

### Presentation Layer

The presentation layer consists of:

- **HTML Structure** (`index.html`): Minimal shell for mounting the Yew app
- **CSS Styling** (`styles.css`): Visual styling, animations, and effects
- **Dynamic HTML**: Generated by Yew's virtual DOM

### Application Layer

The application layer (`src/main.rs`) contains:

1. **Component Definition**: `App` function component
2. **State Management**: React-style hooks (`use_state`, `use_node_ref`)
3. **Event Handling**: Callback creation and state updates
4. **Business Logic**: Animation speed calculations

### Data Layer

The data layer includes:

- **LightColor Enum**: Type-safe color representation
- **State Variables**: Runtime application state
- **Constants**: Light pattern array (8 lights)

## Component Interaction Diagram

```mermaid
sequenceDiagram
    participant User
    participant Button
    participant Callback
    participant State
    participant Component
    participant VDOM
    participant CSS

    User->>Button: Click "On" Button
    Button->>Callback: Trigger on_play
    Callback->>State: Set is_playing = true
    State->>Component: State Changed
    Component->>VDOM: Re-render
    VDOM->>CSS: Remove "paused" class
    CSS->>User: Display Animation

    User->>Button: Click "Off" Button
    Button->>Callback: Trigger on_stop
    Callback->>State: Set is_playing = false
    State->>Component: State Changed
    Component->>VDOM: Re-render
    VDOM->>CSS: Add "paused" class
    CSS->>User: Stop Animation
```

## Rendering Pipeline

```mermaid
flowchart LR
    subgraph Input[Input Data]
        LIGHTS_ARR[lights: Vec of LightColor]
        IS_PLAY[is_playing state]
        SPD[speed state]
    end

    subgraph Process[Processing]
        CALC[Calculate animation_duration]
        MAP[Map over lights array]
        CLASSES[Generate CSS classes]
        STYLES[Generate inline styles]
    end

    subgraph Output[HTML Output]
        LIGHT_DIVS[8 div.circle elements]
        BUTTON_DIV[Control buttons]
        TITLE_DIV[Title heading]
    end

    LIGHTS_ARR --> MAP
    IS_PLAY --> CLASSES
    SPD --> CALC
    CALC --> STYLES
    MAP --> LIGHT_DIVS
    CLASSES --> LIGHT_DIVS
    STYLES --> LIGHT_DIVS
    BUTTON_DIV --> Output
    TITLE_DIV --> Output
```

## Animation Architecture

The animation system uses CSS keyframes with dynamic control:

```mermaid
graph TD
    subgraph CSSLayer[CSS Layer]
        KEYFRAMES[Keyframe: blink]
        DELAYS[Animation Delays]
        DURATION[Animation Duration]
    end

    subgraph RustControl[Rust Control]
        SPEED_STATE[Speed State 1-5]
        PLAY_STATE[Play State bool]
    end

    subgraph HTMLElements[HTML Elements]
        RED[Red Light - 0s delay]
        YELLOW[Yellow Light - 0.25s delay]
        BLUE[Blue Light - 0.5s delay]
        GREEN[Green Light - 0.75s delay]
    end

    SPEED_STATE -->|Maps to duration| DURATION
    PLAY_STATE -->|Controls paused class| KEYFRAMES
    DURATION --> RED
    DURATION --> YELLOW
    DURATION --> BLUE
    DURATION --> GREEN
    KEYFRAMES --> RED
    KEYFRAMES --> YELLOW
    KEYFRAMES --> BLUE
    KEYFRAMES --> GREEN
    DELAYS --> RED
    DELAYS --> YELLOW
    DELAYS --> BLUE
    DELAYS --> GREEN
```

## Build Architecture

```mermaid
graph TB
    subgraph Source[Source Files]
        RUST[main.rs]
        HTML[index.html]
        CSS[styles.css]
        CARGO[Cargo.toml]
    end

    subgraph BuildTools[Build Tools]
        RUSTC[rustc compiler]
        TRUNK[Trunk bundler]
        WASM[wasm-bindgen]
    end

    subgraph Output[Build Output]
        WASM_FILE[app.wasm]
        JS_GLUE[JavaScript glue]
        DIST_HTML[dist/index.html]
        DIST_CSS[dist/styles.css]
    end

    RUST --> RUSTC
    CARGO --> RUSTC
    RUSTC --> WASM
    WASM --> WASM_FILE
    WASM --> JS_GLUE
    HTML --> TRUNK
    CSS --> TRUNK
    TRUNK --> DIST_HTML
    TRUNK --> DIST_CSS
    WASM_FILE --> TRUNK
    JS_GLUE --> TRUNK
```

## Design Patterns

### Component Pattern

The application uses Yew's functional component pattern:

- **Function Components**: Stateless function that returns HTML
- **Hooks**: React-style hooks for state management
- **Props**: None (single component app)

### Observer Pattern

State changes automatically trigger re-renders:

- State changes → Component re-evaluation → Virtual DOM update → Real DOM patch

### Strategy Pattern

Animation speed is determined by a strategy pattern:

- Speed value (1-5) maps to duration string
- Implemented via match expression
- Easily extensible for new speeds

## Security Considerations

### Input Validation

- Speed input is validated to range 1-5
- Type checking ensures i32 parsing
- Invalid inputs are silently ignored

### Memory Safety

- Rust's ownership system prevents memory leaks
- No unsafe code blocks
- WASM sandbox provides additional isolation

---

**Related Pages**:
- [[Component-Details]] - Deep dive into each component
- [[Build-and-Deployment]] - Build process details
- [[Home]] - Return to overview
